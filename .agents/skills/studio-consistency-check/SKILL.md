---
name: studio-consistency-check
description: "Game studio workflow: Scan all GDDs against the entity registry to detect cross-document inconsistencies: same entity with different stats, same item with different values, same formula with different variables. Grep-first approach — reads registry then targets only conflicting GDD sections rather than full document reads."
---

<!-- Generated by scripts/sync_codex.py; do not edit. -->

# Studio: consistency-check

1. Read the [Codex runtime guide](../../../docs/codex/runtime.md).
2. Read the [shared workflow](../../../.claude/skills/consistency-check/SKILL.md) and follow its phases using that guide's translations. Read referenced documents only when the current phase needs them.

Use text following `$studio-consistency-check` as the workflow arguments. Translate references to `/consistency-check` into `$studio-consistency-check`; translate other studio commands the same way. Do not invoke Claude-only tools or treat its frontmatter as Codex settings.
