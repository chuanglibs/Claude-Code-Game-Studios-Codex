---
name: studio-setup-engine
description: "Game studio workflow: Configure the project's game engine and version. Pins the engine in CLAUDE.md, detects knowledge gaps, and populates engine reference docs via WebSearch when the version is beyond the LLM's training data."
---

<!-- Generated by scripts/sync_codex.py; do not edit. -->

# Studio: setup-engine

1. Read the [Codex runtime guide](../../../docs/codex/runtime.md).
2. Read the [shared workflow](../../../.claude/skills/setup-engine/SKILL.md) and follow its phases using that guide's translations. Read referenced documents only when the current phase needs them.

Use text following `$studio-setup-engine` as the workflow arguments. Translate references to `/setup-engine` into `$studio-setup-engine`; translate other studio commands the same way. Do not invoke Claude-only tools or treat its frontmatter as Codex settings.
