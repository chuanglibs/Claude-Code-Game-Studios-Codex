---
name: studio-prototype
description: "Game studio workflow: Concept prototype — validate the core idea is worth designing before writing GDDs. Run right after /brainstorm and /setup-engine. Routes to HTML, Engine, or Paper path based on game type. Produces a throwaway build and a PROCEED/PIVOT/KILL verdict."
---

<!-- Generated by scripts/sync_codex.py; do not edit. -->

# Studio: prototype

1. Read the [Codex runtime guide](../../../docs/codex/runtime.md).
2. Read the [shared workflow](../../../.claude/skills/prototype/SKILL.md) and follow its phases using that guide's translations. Read referenced documents only when the current phase needs them.
3. Use the `prototyper` role from [its shared brief](../../../.claude/agents/prototyper.md). For delegated work, its native Codex name is `studio-prototyper`.

Use text following `$studio-prototype` as the workflow arguments. Translate references to `/prototype` into `$studio-prototype`; translate other studio commands the same way. Do not invoke Claude-only tools or treat its frontmatter as Codex settings.
