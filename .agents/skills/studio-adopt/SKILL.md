---
name: studio-adopt
description: "Game studio workflow: Brownfield onboarding — audits existing project artifacts for template format compliance (not just existence), classifies gaps by impact, and produces a numbered migration plan. Run this when joining an in-progress project or upgrading from an older template version. Distinct from /project-stage-detect (which checks what exists) — this checks whether what exists will actually work with the template's skills."
---

<!-- Generated by scripts/sync_codex.py; do not edit. -->

# Studio: adopt

1. Read the [Codex runtime guide](../../../docs/codex/runtime.md).
2. Read the [shared workflow](../../../.claude/skills/adopt/SKILL.md) and follow its phases using that guide's translations. Read referenced documents only when the current phase needs them.
3. Use the `technical-director` role from [its shared brief](../../../.claude/agents/technical-director.md). For delegated work, its native Codex name is `studio-technical-director`.

Use text following `$studio-adopt` as the workflow arguments. Translate references to `/adopt` into `$studio-adopt`; translate other studio commands the same way. Do not invoke Claude-only tools or treat its frontmatter as Codex settings.
